'use client';
import { useQuery } from '@tanstack/react-query'; import { Heart } from 'lucide-react'; import { MarketplaceShell } from '@/components/layout/marketplace-shell'; import { AccountSidebar } from '@/components/account/account-sidebar'; import { ListingCard } from '@/components/listings/listing-card'; import { socialService } from '@/services/social.service';
export default function SavedPage(){const q=useQuery({queryKey:['saved-listings'],queryFn:()=>socialService.getSaved()});return <MarketplaceShell><main className="mx-auto max-w-7xl px-4 py-5 pb-28 sm:px-6 sm:py-8 lg:px-8 lg:pb-10"><div className="grid min-w-0 gap-4 sm:gap-6 lg:grid-cols-[230px_minmax(0,1fr)]"><AccountSidebar/><section><h1 className="text-2xl font-extrabold">Saved listings</h1><p className="mt-1 text-sm text-slate-500">Keep track of products you may want to revisit.</p>{q.isLoading?<div className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{[1,2,3].map(i=><div key={i} className="h-80 animate-pulse rounded-2xl bg-slate-100"/>)}</div>:q.data?.length?<div className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{q.data.map(l=><ListingCard key={l.id} listing={l}/>)}</div>:<div className="mt-8 rounded-3xl border border-dashed p-12 text-center"><Heart className="mx-auto size-10 text-slate-300"/><h2 className="mt-4 font-bold">No saved listings yet</h2><p className="mt-1 text-sm text-slate-500">Tap the heart on a listing to save it here.</p></div>}</section></div></main></MarketplaceShell>}
